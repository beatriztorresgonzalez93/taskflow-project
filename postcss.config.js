// Configuracion de PostCSS del proyecto.
// Ejecuta plugins de build CSS: Tailwind (generacion de utilidades) y
// Autoprefixer (prefijos de compatibilidad entre navegadores).
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};